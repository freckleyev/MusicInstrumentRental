{% extends 'dashboard/dashboard.html.twig' %}

{% block title %}Edit Categories{% endblock %}

{% block dashboard_stats %}
{% endblock %}

{% block dashboard_body %}
    <h1>Edit Category #{{category.id}}</h1>

    {{ include('dashboard/categories/_form.html.twig', {'button_label': 'Update'}) }}
    <br>
    {{ include('dashboard/categories/_delete_form.html.twig') }}
    <br>
    <a href="{{ path('app_categories_index') }}" class="btn btn-secondary">Back</a>
{% endblock %}
